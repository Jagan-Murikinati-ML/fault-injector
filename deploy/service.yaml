apiVersion: v1
kind: Service
metadata:
  name: chaos-platform-service
  namespace: chaos-platform
spec:
  selector:
    app: chaos-platform
  ports:
  - port: 8080
    targetPort: 8080
  type: LoadBalancer